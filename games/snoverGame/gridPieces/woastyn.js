//frisian for desert
var stuff = [
    //#region starterhouse
    //#endregion
];

mapInfo.tacks.push(new mapTack(3275, 1729, "Woastyn", bilder.mapStar))

for(var i=0; i<stuff.length; i++){
    tree.insert(stuff[i]);
}